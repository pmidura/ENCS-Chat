enum StatusMediaTypes {
  textActivity,
  imageActivity,
}

enum ConnectionStateName {
  connect,
  pending,
  accept,
  connected,
}

enum ConnectionStateType {
  buttonNameWidget,
  buttonBorderColor,
  buttonOnlyName,
}

enum OtherConnectionStatus {
  requestPending,
  invitationCame,
  invitationAccepted,
  requestAccepted,
}
